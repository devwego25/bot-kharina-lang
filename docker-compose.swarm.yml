# Docker Swarm Stack for Kharina Bot
# Deploy: docker stack deploy -c docker-compose.swarm.yml kharina-bot-v2

version: '3.8'

services:
  # Python LangChain Agent
  langchain:
    image: kharina-langchain-v2:latest
    build:
      context: ./python-langchain
      dockerfile: Dockerfile
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - REDIS_URL=redis://redis:6379
      - MCP_CARDAPIO_URL=http://cardapio_app:3000/mcp
      - MCP_CARDAPIO_TOKEN=${MCP_CARDAPIO_TOKEN}
      - MCP_RESERVAS_URL=${MCP_RESERVAS_URL}
      - MCP_RESERVAS_TOKEN=${MCP_RESERVAS_TOKEN}
      - DEBUG=false
      - LOG_LEVEL=INFO
      - HTTP_PROXY=${HTTP_PROXY}
      - HTTPS_PROXY=${HTTPS_PROXY}
      - NO_PROXY=langchain,kharina-bot-v2_langchain,redis,kharina-bot-v2_redis,cardapio_app,localhost,127.0.0.1,172.17.0.1,${NO_PROXY}
    networks:
      - kharina-network
      - KharinaNet  # To reach cardapio_app
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 5s
        order: stop-first
        failure_action: rollback
        monitor: 20s
      rollback_config:
        parallelism: 1
        delay: 5s
        order: stop-first
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Node.js Backend (refactored)
  backend:
    image: kharina-backend-v2:latest
    build:
      context: ./nodejs-bot/backend
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
      - PORT=3000
      - WHATSAPP_TOKEN=${WHATSAPP_TOKEN}
      - WHATSAPP_PHONE_ID=${WHATSAPP_PHONE_ID}
      - WHATSAPP_VERIFY_TOKEN=${WHATSAPP_VERIFY_TOKEN}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=redis://redis:6379
      - LANGCHAIN_URL=http://langchain:8000
      - CHATWOOT_URL=${CHATWOOT_URL}
      - CHATWOOT_TOKEN=${CHATWOOT_TOKEN}
      - CHATWOOT_ACCOUNT_ID=${CHATWOOT_ACCOUNT_ID}
      - CHATWOOT_INBOX_ID=${CHATWOOT_INBOX_ID}
      - MCP_CARDAPIO_URL=http://cardapio_app:3000/mcp
      - MCP_CARDAPIO_TOKEN=${MCP_CARDAPIO_TOKEN}
      - MCP_RESERVAS_URL=${MCP_RESERVAS_URL}
      - MCP_RESERVAS_TOKEN=${MCP_RESERVAS_TOKEN}
      - HTTP_PROXY=${HTTP_PROXY}
      - HTTPS_PROXY=${HTTPS_PROXY}
      - NO_PROXY=langchain,kharina-bot-v2_langchain,redis,kharina-bot-v2_redis,cardapio_app,localhost,127.0.0.1,172.17.0.1,${NO_PROXY}
    networks:
      - kharina-network
      - KharinaNet
      - traefik-public
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 5s
        order: stop-first
        failure_action: rollback
        monitor: 20s
      rollback_config:
        parallelism: 1
        delay: 5s
        order: stop-first
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.kharina-bot-v2.rule=Host(`chatbot.kharina.com.br`)"
        - "traefik.http.routers.kharina-bot-v2.entrypoints=websecure"
        - "traefik.http.routers.kharina-bot-v2.tls.certresolver=letsencrypt"
        - "traefik.http.services.kharina-bot-v2.loadbalancer.server.port=3000"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Redis (shared)
  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    networks:
      - kharina-network
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

networks:
  kharina-network:
    driver: overlay
    attachable: true
  KharinaNet:
    external: true
  traefik-public:
    external: true

volumes:
  redis-data:
